# Nginx 反向代理配置
# 用于将外部请求转发到本地的 FastAPI 和 Gradio 服务

# 修改以下配置：
# 1. 将 your_domain_or_ip 替换为你的域名或服务器 IP
# 2. 如果使用域名，需要先解析到服务器 IP

server {
    listen 80;
    server_name your_domain_or_ip;

    # 日志配置
    access_log /var/log/nginx/industrial-ai-agent-access.log;
    error_log /var/log/nginx/industrial-ai-agent-error.log;

    # 最大上传文件大小
    client_max_body_size 50M;

    # API 服务（FastAPI）
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时设置
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # API 文档
    location /docs {
        proxy_pass http://127.0.0.1:8000/docs;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Web 界面（Gradio）
    location / {
        proxy_pass http://127.0.0.1:7860;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Gradio WebSocket 支持
        proxy_buffering off;
        proxy_cache off;
    }
}

# HTTPS 配置（使用 Let's Encrypt 免费 SSL 证书）
# 运行以下命令自动配置：
# sudo certbot --nginx -d your_domain.com

# server {
#     listen 443 ssl http2;
#     server_name your_domain_or_ip;
#
#     ssl_certificate /etc/letsencrypt/live/your_domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your_domain.com/privkey.pem;
#
#     # SSL 优化配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # 其他配置与 HTTP 相同
#     # ...
# }